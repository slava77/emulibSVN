Relax NG schema definitions are converted from the original .xsd files as follows

.xsd --> .rng
xsltproc XSDtoRNG.xsl parameter.xsd > parameter.rng
xsltproc XSDtoRNG.xsl parameterClassifiers.xsd > parameterClassifiers.rng

.rng --> .rnc
trang parameter.rng parameter.rnc

These can then be used to validate documents in nxml-mode in emacs.

References:
XSDtoRNG.xsl is from http://code.google.com/p/xsdtorngconverter/
trang is from http://code.google.com/p/jing-trang/
