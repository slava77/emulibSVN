#!/bin/sh
#
# This script will be executed *after* all the other init scripts.
# You can put your own initialization stuff in here if you don't
# want to do the full Sys V style init stuff.

touch /var/lock/subsys/local
##/home/dcs/bin/dcscontrol_watch_dog &
/home/fast/bin/dns_wrap &
mkdir /mnt/central_installation
mkdir /mnt/central_repository
mount -t nfs pcmsucr3:/central_dcs_installation /mnt/central_installation &
mount -t nfs pcmsucr3:/central_dcs_repositoty_csc /mnt/central_repository &